//bài tập 1//
function multiply(a, b) {
    console.log(`${a}*${b} = ${a * b}`);
};
multiply(1, 2);
multiply(5, 6);

//bài tập 2//

//bài tập 3//


//bài tập 4//
function calculateInterest(principal, rate, years) {
    let total = (`${principal + principal * rate * years / 100}`);
    console.log(total);
};
calculateInterest(100, 50, 2)